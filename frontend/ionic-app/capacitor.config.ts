import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-react-typescript-boilerplate',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
